# Discovered candidates. Scheduled research appends here only after deduplication and syntax review.
# Every entry must remain enabled=false until manually promoted.
# Format:
# URL, tag=候选-名称, update-interval=259200, opt-parser=true, inserted-resource=true, enabled=false
